# Database Configuration
DATABASE_URL=postgresql://your_username:your_password@localhost:5432/auth_db

# Server Configuration
PORT=5001
NODE_ENV=development

# JWT Secret Key (Change this to a strong random string)
JWT_SECRET=your_super_secret_jwt_key_change_this_to_something_random

# Email Configuration (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password
CLIENT_URL=http://localhost:3000

# Admin Configuration
# Users with this email will automatically have admin privileges
ADMIN_EMAIL=your-admin@gmail.com

# Notes:
# 1. DATABASE_URL: Update with your PostgreSQL credentials
# 2. JWT_SECRET: Generate a strong random string (e.g., use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# 3. SMTP_USER & SMTP_PASS: Use Gmail App Password (not your regular Gmail password)
#    - Enable 2FA on your Google account
#    - Go to: Google Account → Security → 2-Step Verification → App passwords
#    - Generate a password for "Mail"
# 4. ADMIN_EMAIL: Set to your email to get admin access
